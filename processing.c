#include<stdio.h>
int main()
{
    //Processing sikhbo

    int a,b;
    printf("Enter two number: ");
    scanf("%d %d",&a,&b);

    int sum,sub;
    sum = a+b;
    sub = a-b;
    printf("The sum of two numbers: %d",sum);
    printf("The sub of two numbers: %d",sub);
  return 0;
}
